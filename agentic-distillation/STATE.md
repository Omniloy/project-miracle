# Agentic distillation of Qwen3.8-27B — project state

_Last updated: 2026-09-03 19:30 UTC (auto-saved periodically; see `save_state.sh`)._

## Objective
Produce the best open-weights model at its size on the Artificial Analysis **Agentic Index** (= mean of **GDPval-AA v2** and **τ³-Bench Banking**), by distilling stronger teachers into **Qwen3.8-27B**, with a clear measured margin over the base on both benchmarks, reproducibly via the public harnesses. Stretch: make it *smaller* (depth-prune + heal) while better.

## Where things live
| What | Where |
|---|---|
| Code (this dir) | `agentic-distillation/pipeline/**` — task generator, validator, decontamination, verifiers, SFT converters, GPU job scripts |
| Research briefs | `agentic-distillation/research/*.md` (compression brief, agentic plan, GLM brief) |
| Private HF work repo (data, trajectories, adapters, results) | `Enriqueag26/agentic-distill-work` (dataset, private): `bundle/`, `data/` (v0), `data_v1/`, `bundle/data_synth_tasks/` (79 synthetic tasks), `status/`, `adapter_v0/`, `results/` |
| Harness | `sierra-research/tau2-bench` v1.0.1 (+ `harness/tau2_config_openrouter_judge.patch`: routes NL judge / env-interface / review LLMs through OpenRouter) |
| Secrets | **never in the repo**; kept in the session scratchpad `.env` (OPENROUTER_API_KEY, VAST_API_KEY, HF_TOKEN). Rotate after the project. |

## Key numbers so far (all measured by us unless noted)
- **Baseline Qwen3.8-27B on τ³-Banking**, AA-exact config (`bm25_grep`, seed 300, GPT-5.4 Mini medium user-sim, 200 steps, 1 trial): **44/97 = 45.4% pass^1** (AA: 47.4 medium / 48.0 xhigh) → harness reproduces AA within noise. Banking has **0/97 NL-assertion tasks** (DB/ACTION rewards only) so the judge model is irrelevant there.
- AA reference (2026-09-03): Banking — Muse Spark 1.3 52.4, Qwen3.8-Max 51.3, GLM-5.3 50.3, **Qwen3.8-27B 48.0 (#3)**, GLM-5.3-Flash 47.2. GDPval-AA v2 — Fable 5.1 1853, GLM-5.3-Flash 1765, GLM-5.3 1758, Qwen3.8-Flash-Next 1743, Qwen3.8-Max 1721, **Qwen3.8-27B 1543**. ⇒ Banking headroom ~3 pts; GDPval headroom ~200 Elo.
- **Teacher memorization probe**: 0/12 exact tool-suffix hits for Qwen3.8-Max, GLM-5.3, GLM-5.3-Flash without KB access → no evidence teachers memorized the public test set.
- Synthetic tasks (real Rho-Bank KB, fresh entities, replay-validated, decontaminated): **79 live** (batch1: 27 GLM + 12 Max + 5 pilot; batch2 harder: 35 GLM). Generation ≈ $0.03–0.06/task.
- Rollouts (relaxed outcome verifier = DB state minus read-log table and timestamp): GLM-5.3 29/64 on batch1; GLM-5.3 10/24 on Max-authored tasks; Qwen3.8-Max 3/30 on the 15 GLM-unsolved tasks; **Qwen3.8-Flash-Next 0/28** on the same; student 16/18 on GLM-solved tasks, ~42% overall. **13/15 "hard" tasks unsolved by every agent → treat as broken (solvability gate).**
- SFT v0: 42 verified trajectories → 37 train / 5 dev (3 held-out tasks) → 68 windows (≤16K tokens, tool results capped at 8k chars), 805 trained assistant turns. v1 adds student self-passes (auto-built when the self-rollout run exits).

## Decisions (with reasons)
1. **Student = Qwen3.8-27B** (Apache-2.0, 55.6 GB bf16 / 30.9 GB FP8, `model_type qwen3_5`, trainable on one 96 GB GPU). Not GLM: GLM-5.3 is 753B API-only, GLM-5.3-Flash is 328 GB bf16 and *below* the 27B on Banking; GLM-4.7-Flash (31B-A3B) has Intelligence Index 23 vs 52. Not Qwen3.8-Flash-Next: 360 GB bf16 / 135 GB NVFP4; experts are fused 3-D tensors → PEFT/bitsandbytes cannot LoRA/QLoRA them; no trainer supports `qwen4_exp`; 0/28 on our hard set; deployable artifact 135–185 GB contradicts "smaller".
2. **Teachers**: GLM-5.3 primary (newest, strongest on GDPval), Qwen3.8-Max on residual failures, Qwen3.8-Flash-Next as cheap 3rd teacher for GDPval-style tasks only. GLM-5.3-Flash = user-simulator / cheap judge (it is below the student on Banking).
3. **Verifier-first, multi-teacher cascade** (user's strategy): every trajectory kept only if the environment's DB-state comparison passes; union of verified successes from any teacher + the student's own verified passes (self-distillation). Tasks nobody solves are dropped, not trained toward.
4. **Contamination**: never train on the 97 test tasks or paraphrases; synthetic tasks decontaminated by 5-gram Jaccard, identical gold-action signature, and entity overlap (names/emails/ids/phones) — disclose "Track B (real KB, fresh entities)" in the model card.
5. **GPU**: RTX PRO 6000 Blackwell 96 GB at ~$1.3–1.6/hr (kernels verified: `flash-linear-attention` imports; torch 2.12.1+cu130, transformers 5.16.1, peft 0.20.0). H100 only if DeltaNet LoRA fails on Blackwell. **No SSH egress from the controller container** → all GPU jobs are unattended `--onstart` scripts that pull from / push to the HF work repo; monitored via `vastai logs` + HF `status/`.
6. **Training**: bf16 LoRA r=64/α=128 on attention+MLP (+DeltaNet `linear_attn.in_proj_qkv/in_proj_z/out_proj` if the smoke test trains them), Axolotl, seq 16K, `enable_thinking: false` (teacher trajectories carry no reasoning; AA shows Banking insensitive to thinking budget), eval both modes.

## Gotchas discovered (save time later)
- The benchmark DB hash also covers a **frozen clock** (`get_current_time` = `2025-11-14 03:40:00 EST`; verification-record id derives from it) and a **read-log table** of allowlisted lookup calls (allowlist = reads present in the task's gold actions). Synthetic gold actions must use the frozen timestamp; use `relaxed_verify.py` (ignores read-log table + timestamp) for training-data filtering only.
- tau2 loads only `task_*.json` files from `data/tau2/domains/banking_knowledge/tasks/`; use `TAU2_DATA_DIR=<parallel dir>` with symlinked `documents/`, `db.json`, `prompts/`, `user_simulator/` to run synthetic tasks.
- LiteLLM cost-table "isn't mapped yet" errors are cosmetic; the default NL judge (`gpt-4.1` direct OpenAI) needs the patch to route via OpenRouter (matters for retail/airline, not Banking).
- Teachers sometimes nest `evaluation_criteria` under `initial_state`, return list-valued `relevant_policies`, dict personas, or invent tables → `normalize()` in the generator + validator coercions handle these.
- Qwen3.8-Max/GLM with thinking on exhaust `max_tokens` on long JSON outputs → `reasoning.effort=low`, `max_tokens 16000`.
- OpenRouter rate-limits `qwen/qwen3.8-flash` heavily at concurrency 8.

## GPU run findings (v0, 2026-09-03 ~20:20 UTC onward)
- RTX PRO 6000 Blackwell (sm_120): `flash-linear-attention` 0.4.1 imports and trains; **`flash_attn` and `causal_conv1d` were not installed** in `axolotlai/axolotl-cloud:main-latest` → attention fell back to SDPA and DeltaNet conv to the slow path → **~85 trained tok/s, ~20 min per optimizer step** (17 steps ≈ 6 h ≈ $8 for v0). Peak memory 82 GB at ~13K-token samples, so 16K windows fit on 96 GB with the DeltaNet LoRA targets included (PEFT matched `linear_attn.in_proj_qkv/in_proj_z/out_proj`).
- v0 training signal: loss 0.47 → 0.44 over the first 3 steps; eval ppl 1.63 before training.
- The standalone smoke script crashed on tokenizer input (chat template rejected the tool-call rows outside Axolotl's normalizer) — fixed; Axolotl's own preprocessing handles the rows.
- **SFT v1 assembled**: 97 verified trajectories over 46 tasks (GLM-5.3 39, Max 3, student 55) → 84 train / 13 dev (6 held-out tasks) → **161 windows (~3.7M tokens)**; on HF at `data_v1/`. For v1 training: install `causal-conv1d` + `flash-attn` at job start (time-capped) or use an H100 if wheels are unavailable for sm_120.
- Solvability gate on batch 2: student solved 21/35 tasks (38/70 episodes relaxed); GLM-5.3 solved **0/14** of the student's failures. Across all 79 synthetic tasks: **46 solved by ≥1 agent, 33 solved by nobody → dropped** (`runs/solvability_gate.json`). A 42% unsolvable rate means the generator's gold actions are often not reproducible (mismatches concentrate in `credit_limit_increase_requests`, `credit_card_accounts`, `accounts`); diagnosing before generating batch 3.
- Batch-2 failure analysis (gold vs agent DB diffs): four generator flaws — duplicate/contradictory gold writes (submit twice; approve AND deny), inconsistent initial state (credit-limit task without a credit-card row), free-text args stored in rows (`reason`), `log_verification` for a different user id. Fixes: `relaxed_verify.py` v2 compares rows by content ignoring free-text fields and order-dependent row ids; `validate_task.py` adds consistency checks (duplicates, approve+deny, unknown entity ids, verification user id); generator prompt rule 9. The static checks catch only 3/13 batch-2 unsolvable tasks — the rest are wrong gold *decisions*, so the empirical solvability gate stays mandatory.
- Solvability gate v2 (content-based verifier): **48 solvable / 31 unsolvable** of 79. Verified trajectories: **133** (student 83, GLM-5.3 41, Max 9) → **SFT v1 = 79 train / 17 dev trajectories, 157 train windows (~3.5M tokens)** on HF `data_v1/`.
- Gating policy from batch 3 on: student N=2 first (cheap, self-distillation) → Qwen3.8-Max N=1 only on the student's failures → drop the rest. (GLM-5.3 solved 0/14 of the student's batch-2 failures; teacher-on-failures is low-yield.)
- Batch 3 (stricter validator + prompt rule 9): 40 generated → 31 clean → **20/31 solvable** (65% vs 58% for batch 2) under the student-first gate (student 32/62 episodes; Max 3/14 on the student's failures). **Overall: 110 synthetic tasks, 68 solvable, 42 dropped.**
- **SFT v1 (final for this round): 168 verified trajectories over 68 tasks → 113 train / 18 dev trajectories (10 held-out tasks) → 216 train windows (~4.8M tokens), 48 dev windows**; on HF `data_v1/`. Sources: student 130, GLM-5.3 25 (after per-task caps), Max 8.
- Data generation paused here to reserve budget for evaluation (dev + 97-task test for base and adapter; 5-trial final ≈ $95).
- OpenRouter spend at this point: ~$126 (of ~$300) (rollouts dominate; each teacher episode ≈ $0.4–0.7, student ≈ $0.15, user-sim Flash ≈ $0.02).

## Gold repair (2026-09-04, human-in-the-loop = Claude reading the policy docs)
- Of the 42 tasks no model solved, I read the governing KB documents for five and found **the models were right and the gold was wrong** in each: synth_1018 (KB lists `late_fee_reversal` for a late fee; gold used generic `goodwill_adjustment`), synth_1021 (logistics_003 Step 3 requires `log_credit_card_closure_reason` before closing — gold omitted it), synth_1004 (doc_031 maps card-not-present fraud → `close_and_reissue` and requires the agent to perform it; gold said freeze and omitted close+reorder; liability tier $500 not $50), synth_1016 and synth_2012 (no customer rows injected at all). Repairs in `pipeline/banking/repair_tasks.py`, all 5 replay-valid.
- Re-scoring the agents' EXISTING trajectories against repaired gold: **14/20 pass** (every agent had solved 1018 and 1021; Max had solved 1004) → 14 recovered training trajectories at zero cost. Student rollouts launched on the two customer-less tasks.
- Consensus scan of the remaining 37: **27 have ≥2 agents agreeing on an identical write-set that differs from gold** (`runs/gold_repair_candidates.json`). Recurring classes: (a) agents close the old card before ordering a replacement (1026, 3033, 4035, 4005); (b) same tools, argument-level differences (1008, 3014, 3025, 3031, 4011, 4013, 1025); (c) approve-vs-deny credit-limit judgments (3000, 3036, 3037); (d) agents transfer/give up (1014, 4026, 4031, 3002, 3004). Classes (a)/(b) are likely more gold errors; (c)/(d) need policy reading.
- Lesson for the paper: teacher-authored gold is the weakest link; a "judge by policy" repair pass is cheap and recovers real data. SFT v1 now 111 train / 23 dev trajectories, 213 windows.
- Repaired-task rollouts (student N=2): synth_1016 solved 2/2 (relaxed+strict); synth_2012 0/2 and Qwen3.8-Max 0/1 — every agent decides the statement-credit tool is not for rewards redemption and transfers; policy (`business_bronze_rewards_card_002/004`: "Redemptions are applied to your account, typically as a statement credit"; `credit_cards_(general)_017` lists `promotional_credit`/`other`) arguably supports the gold → left in the adjudication queue rather than repaired.
- **SFT v1 rebuilt with the dev split pinned** (`assemble_sft.py --holdout-file runs/sft_v1/split_prev.json`; before this, adding a task reshuffled the held-out set): 184 eligible trajectories over 72 tasks → **113 train / 23 dev, 215 train windows, 56 dev windows**, only synth_1016 new in train; on HF `data_v1/`. Gate v2 file updated: 72 solvable / 38 unsolved.
- **Adjudication of the 38 unsolved tasks — DONE (2026-09-04 07:00–07:50 UTC)**: `pipeline/banking/case_files.py` builds one case file per task (scenario, injected rows, gold, every agent trajectory's write-set + final message, required docs) in `runs/case_files/`; six parallel reviewers (same model, ~1.35M tokens total) followed `runs/rulings/INSTRUCTIONS.md` and wrote `runs/rulings/<task>.json` (verdict, quoted policy basis, full corrected action list, optional DB-row injections / user-instruction patch). **Verdicts: 30 GOLD_WRONG, 2 GOLD_CORRECT with data bugs (4002 missing credit_limit, 4026 wrong expiry row), 6 AMBIGUOUS → dropped (1012, 1015, 2012, 3009, 3023, 3032).** Applied with `apply_rulings.py` (task backups in `runs/task_backups/`), all replay-valid (`validate_task.py`), then `rescore.py` re-scored every existing trajectory against the repaired gold: **64 trajectories over 18 tasks now pass at zero rollout cost** (0102, 1008, 1025, 1026, 1035, 1038, 2001, 2015, 2017, 3000, 3004, 3014, 3025, 3033, 3036, 3037, 4035, 4036). Student N=2 rollouts running on the 15 repaired tasks with no passing trajectory (`repaired_student_r1`/`_r2`).
- **Recurring gold-error classes (paper material)**: (1) tier tables ignored (Light Blue = entry tier → standard shipping only; Silver Rewards = mid tier); (2) the environment's own preconditions ignored (order_debit_card refuses while a card is ACTIVE → close first; close_bank_account refuses unless balance − fee = 0); (3) mandated follow-through steps omitted (doc 031: card_not_present_fraud ⇒ close_and_reissue AND actually close the card; doc 007 Step 1: submit the CLI request before evaluating it; doc 025: advise disputes); (4) the customer's own wrong numbers adopted as gold (APY boosts that don't exist, 2.775%; fee amounts; "5x promo" points); (5) cooldown after a DENIED request (doc 005 says none) — reviewers consistently ruled by doc 005; (6) fabricated last-4 digits where the user tool returns a deterministic sha256-derived value; (7) initial-state shape bugs (`agent_data` without the `data` wrapper, tables nested inside `users`, undocumented account levels like "Premier Checking").
- **Harness bug found by the reviewers: synthetic tasks with user-side tools listed the discoverable tool's name in `user_tools` instead of `call_discoverable_user_tool`** (what all real tasks use), so the user simulator never had the meta-tool and 0 of ~425 synthetic simulations contained a user tool call. The real benchmark relies on it: 21/97 test tasks have user-tool gold and the base student passes only 6 of them — a prime training target. Fixed by `pipeline/banking/fix_user_tools.py` (sets `user_tools`, restores the give/call pair, and rewrites the last-4 digits to the tool's real output: 3031→7312, 4010→0408, 4011→9796, 4013→5835, 4030→0091); `validate_task.py` now accepts the meta-tool; generator must set `user_tools=["call_discoverable_user_tool"]` for such tasks (TODO in `gen_banking_tasks.py`).
- Student N=2 on the 14 repaired tasks without a passing trajectory (`repaired_student_r1/r2`, ~$4): **12/14 solvable, 21 new verified trajectories**; the user simulator now calls its tools (e.g. 4010/4013/4030 each show 2–3 user tool calls). Left unsolved: 3016 (student and Max both pick the $500 liability tier; report within 2 business days ⇒ $50) and 4011 (both reuse the stale last-4 from an old dispute row instead of handing the customer `get_card_last_4_digits`) — gold is right, kept as hard traps.
- **Second pass by Claude directly on the 6 AMBIGUOUS + 2 unsolved tasks (09:00 UTC)** — the batch reviewers were the same model but I had only spot-checked them, so I re-read the docs myself:
  - synth_3016 (gold correct, trap): doc 031 ties liability to when the customer *noticed* (11/12); reported 11/14 = within 2 business days ⇒ $50. Student and Max both wrote $500 (counting from the 11/10 transaction). Kept as a hard case.
  - synth_4011 (data fix): the injected prior-dispute row carried an invented last-4 (7742) while the user tool returns 9796; every agent trusted the row. Aligned the row; re-rolling.
  - synth_1012 (fixable): `apply_savings_account_credit_6831` rejects class `business_savings` (base DB has none) → account class set to `savings`; gold math ($40k × (5.45−5.0)%/12 = $15.00) unchanged; re-rolling.
  - synth_1015 (fixable): denied requests carry no cooldown (logistics_005) and only 2 on-time months exist ⇒ `insufficient_payment_history`; `credit_limit` added; the $35 overdraft fee was a mischarge (no personal checking level charges one) → removed, so the $1,600 temporary-limit request is refused on the 150% cap alone and the user now declines a partial increase; re-rolling.
  - synth_3032 (fixable): the Diamond card has no credit limit anywhere, so utilization/cap checks are impossible; made the 10/28 prior request APPROVED so the 60-day premium cooldown (both logistics_005 and _007 agree) decides: `cooldown_period_active`; re-rolling.
  - synth_3009 (drop): "High-Yield Savings" is not a product; repairing it as a Silver Account at $24k with Green checking reproduces synth_1035's numbers exactly (4.0 + 0.25 + 0.025% relationship bonus, which silver_005 states eligible customers receive) — a duplicate, so dropped. This check also confirms the synth_1035 repair.
  - synth_3023 (drop): whether an UNDER_REVIEW cash-back dispute counts as an "active dispute" for a CLI (doc 007 step 2.3) is undefined and the 97 real tasks contain no cash-back-dispute precedent (only two CLI denials exist: task_051 high_utilization, task_052 cooldown after an APPROVED request — which also backs the doc-005 reading used in 6 repairs). My own reading would deny the Gold CLI, contradicting gold and every agent; dropped rather than guessed.
  - synth_2012 (drop): no redemption tool, nothing debits reward_points, and `promotional_credit` is defined as a campaign credit; policy does not decide.
- Second-pass rollouts (student N=2 on 1012/1015/3032/4011): 1015, 3032, 4011 now solvable (5 new trajectories); 1012 still fails (student opens a 'Blue Account' instead of the requested Green checking and grants a $25 goodwill credit the gold declines).
- synth_1012 was underspecified rather than hard (user 'didn't care' which checking class, gold pinned one; $25 goodwill compromise is discretionary) → user scenario now names the class and refuses partial credits → student 1/2.
- **SFT v1 now: 275 eligible trajectories over 106 tasks → 168 train (96 tasks) / 23 dev (10 pinned held-out tasks) → ~392 train windows (~8.9M tokens), 56 dev windows**; on HF `data_v1/`. Gate v2 final: **106 solvable / 4 unsolved (2012, 3009, 3023 underspecified-dropped; 3016 genuine hard trap)** of 110 synthetic tasks — up from 68 solvable this morning, entirely from gold repair.

## v0 training result (2026-09-04 01:10 UTC)
- Completed 17/17 steps in ~5 h on the RTX PRO 6000 (~$7). Train loss 0.47 → **0.25**; eval loss (dev windows) 0.486 → **0.333**. `adapter_v0/` (LoRA r=64, attention+MLP+DeltaNet targets) on HF. Training instance destroyed.
- **Eval attempt 1 FAILED silently (2026-09-04 01:13–07:10 UTC, Vast 49809253, `vllm/vllm-openai:v0.28.0`, ~$7.5 lost)**: the `--onstart-cmd` never ran on that image (no model download, GPU 0%, no `status_eval/` upload). Destroyed. Lesson: use the PyTorch devel image that ran our training onstart (`pytorch/pytorch:2.11.0-cuda12.8-cudnn9-devel`, vLLM pip-installed by the script) and require an early `ALIVE` marker in `status_eval/step_eval.txt` (added to `remote_eval.sh`, plus a 5-min heartbeat with the vLLM log tail). Generic launcher: `pipeline/gpu/launch_vast.sh OFFER SCRIPT DISK "-e K=V ..."` (uploads the script to HF `bundle/` and creates the instance).
- **Eval attempt 2 also dead** (Vast 49832385, `pytorch/pytorch:2.11.0-cuda12.8-cudnn9-devel`, 07:13–07:36 UTC, ~$0.5): same symptom, onstart never ran. Attempt 3 (Vast 49833588, axolotl image) ran the script but **vLLM's engine died: default `max_num_seqs` 1024 > 573 Mamba cache blocks on the 96 GB card** (hybrid DeltaNet needs one Mamba block per decode sequence); the wait loop then timed out and started the eval against a dead server → destroyed (~$0.5). Fix: `--max-num-seqs 32`, and the script now aborts with `STEP vllm FAILED` if the server process dies. **Attempt 4 running: Vast 49838262 (RTX PRO 6000 WS, Japan, $1.30/hr, started 08:35 UTC after a Virginia host sat 30 min in image unpacking) on `axolotlai/axolotl-cloud:main-latest`** — the only image on which our `--onstart-cmd` has ever executed. vLLM 0.28.0 goes into its own venv (`/workspace/vvenv`) so the image's torch stays intact. Sequence: synthetic dev set (Flash user-sim, 2 trials) → **97 real tasks, AA config, 1 trial** → HF `eval_student/`, `status_eval/step_eval.txt` ends with `EVAL_COMPLETE`; the log tail of vLLM is mirrored every 5 min (`status_eval/vllm_tail.log`). Compare with the 45.4 baseline via `pipeline/analyze_eval.py`; base reference on the 10 held-out dev tasks: 12/20 = 60% (relaxed). **Throughput warning**: vLLM 0.28 + LoRA on the hybrid 27B does only ~120 generated tok/s aggregate at 8 concurrent requests (default LoRA kernels, Triton JIT) → ~17 agent calls/min; this run's dev set is the old 88-task definition (~4 h) before the 97-task test (~3 h), ≈ $9 total. `remote_eval.sh` now uses only the 10 pinned holdout tasks for dev (from `$DATA_DIR/split.json`), and the next eval should try `--max-lora-rank 64` with the LoRA merged into the base weights instead (no LoRA kernels) or FP8. **Destroy afterwards** (`vastai destroy instance 49838262 -y`).
- Decision rule: if the adapter is ≥ baseline on test (within CI) and up on dev → train v1 on `data_v1` (216 windows); if it regresses on test → data/format bug, do not scale.

## v0 eval result (2026-09-04 20:07 UTC, Vast 49838262, LoRA path bf16, thinking on, ~11.5 h, ~$15 GPU)
- **Harness bug**: vLLM was started with `--max-model-len 65536`; the real tasks' conversations reach 100k+ tokens (baseline transcripts: median ~110k tokens on the long ones), so **30/97 test tasks and 12/20 dev episodes died mid-conversation with ContextWindowExceeded** (tau2 records them as `infrastructure_error` with an empty transcript). The OpenRouter baseline had a 262k window and 0 such errors. Fixed: `MAXLEN` env, default 262144.
- **On the 67 scored tasks: student 25 vs base 31 (37% vs 46%)**; paired: 4 gained (002, 017, 079, 094), 10 lost; task-bootstrap 90% CI for the delta over all 97 (counting infra as 0) is [-27.8, -11.3] pts, but that is dominated by the 30 unscored tasks. Verdict: **v0 is not an improvement and is probably a small regression** (1 trial; 4 vs 10 flips has p≈0.09).
- Behavioural diff: the student **transfers to a human on 20/67 scored tasks vs 13/67 for the base**; the SFT set contains transfers in 18/168 trajectories (11%; mostly student self-distillation passes on 11 synthetic tasks, 3 copies of synth_3004's emergency transfer). User-tool subset (21 tasks): student 3 pass / 3 infra vs base 6.
- Dev (10 pinned holdout, relaxed): 5/8 scored = 62% vs base 60% — 12 of 20 episodes lost to the same context bug, inconclusive.
- Other mismatches to fix before v1 is judged: (1) **no thinking traces in the SFT data** (all 3,650 assistant turns have empty reasoning; training used `enable_thinking: false`) while the eval runs the student in thinking mode; (2) SFT windows are ≤16k tokens with tool outputs capped at 8k chars, evaluation contexts are 5-10x longer.
- Next eval design (same box, one serving config, `MAXLEN=262144`, LoRA + MTP k=2, 16 streams): **base (thinking on), student (thinking on), student (thinking off)** on the 97 test tasks, 1 trial each — apples-to-apples base reference under our own serving, and a direct test of the thinking-mismatch hypothesis.
- **Running: Vast 49895493 (Spain, $1.44/hr, started 20:17 UTC)**, `RUNS=base:default,student:default,student:off`, results land in HF `eval_student/test_<model>_think<mode>/`. **Reality check on speed**: with the 262k window the long conversations survive and hold 30-100k tokens of KV each, so only 5-10 conversations run at once (KV cache 90% full, 440k-token pool) and the hybrid model's prefix cache hits only ~13% across multi-turn contexts (97% on the short bench prompts) → every turn re-prefills its history. Observed ~7 agent calls/min → **~5-6 h per 97-task run, ~16 h and ~$23 GPU for the three runs** (+ ~$5-8 user-sim each). Accepted rather than restarting: the long tasks are exactly the ones the first eval dropped. Levers for later evals: `--kv-cache-dtype fp8` (2x KV pool), `--gpu-memory-utilization 0.95`, or a paired task subset for cheap A/B comparisons.
- **00:51 UTC check-in: only 1,083 calls in 4.5 h and decelerating (~4/min)** → destroyed 49895493 (~$7 spent, base run ~35% done, discarded). Diagnosis: KV pool full (97%) → cached prefixes evicted between turns → every turn re-prefills 30-100k tokens; prefix hit 10-13%. **Relaunched as Vast 49914721 (Nevada, $1.20/hr, 00:55 UTC)** with `KVDTYPE=fp8` (2x KV pool), `GPU_UTIL=0.95`, `CONC=8` (fewer live conversations so prefixes survive), MTP k=2, MAXLEN 262144, same three runs. New env knobs in `remote_eval.sh`: `KVDTYPE`, `GPU_UTIL`. If fp8 KV fails on the hybrid model → fall back to `KVDTYPE=auto CONC=6`.
- **01:23 UTC: the relaunch fixed it.** fp8 KV loads fine on the hybrid model (FlashInfer XQA decode, KV pool 980k tokens = 3.74x a 262k request); with 8 live conversations the pool sits at ~40%, **prefix-cache hit rate 78% (was 10-13%)**, generation 195-290 tok/s aggregate, no waiting queue, **~19 agent calls/min (was 4-7)**. Lesson for `research/inference_speed.md`: for long multi-turn agent evals the binding constraint is KV *residency between turns*, not decode speed — more concurrency past the point where cached prefixes get evicted makes the whole run slower. ETA: ~2.5 h per 97-task run, ~8 h and ~$10 for the three runs.


## Spend (2026-09-03 19:30 UTC)
OpenRouter ≈ **$85** of the ~$300 distillation budget (baseline $20, teacher/student rollouts ~$55, generation ~$4, probes ~$1). Vast ≈ **$0.5** of $111 (instance #1 destroyed; instance #2 running the smoke+LoRA job at $1.34/hr).

## In flight / next
1. **Eval v0 (Vast 49838262)** → when `EVAL_COMPLETE`: download `eval_student/`, run `pipeline/analyze_eval.py`, destroy the instance, record numbers here.
2. Rollouts on repaired tasks done and folded in (see Gold repair). Remaining: 3016/4011 argument diffs (inspect `runs/rescore_r12.relaxed.json`); generator batch 4 with the new rules (`user_tools` meta-tool, tier tables, env preconditions) if more data is wanted.
3. If adapter_v0 ≥ baseline on test: `launch_vast.sh OFFER remote_main.sh 200 "-e STAGE=train -e DATA_DIR=data_v1 -e RUN_NAME=q38_27b_lora_v1"`, then eval v1; 5-trial final eval (~$95) only for a publishable number.
4. Later: GDPval-style track (Flash-Next / GLM-5.3-Flash teachers, Stirrup harness); depth-prune + heal experiment ("smaller and better").

## Spend (2026-09-04 07:20 UTC)
OpenRouter ≈ **$135** of ~$300 (adjudication used no API budget; reviewers ran in-session). Vast: credit ≈ **$95** left of $111 (training v0 ≈ $7, dead eval attempts ≈ $8, attempt 3 running at $1.27/hr).

## Inference speed (2026-09-04)
- **Why the eval is slow (120 tok/s aggregate, 15 tok/s per stream at 8 concurrent)**: bf16 weight bandwidth (54 GB read per decode step caps a stream at ~33 tok/s; vLLM gets 22-23), a small decode batch (tau2 concurrency 8 leaves the 96 GB card half idle), and one token per step. **Not the LoRA kernels**: merged bf16 decodes at 23.4 vs 22.6 tok/s/stream with the adapter (STATE hypothesis refuted).
- **Benchmark (Vast 49856926, RTX PRO 6000 WS, 46 min, $1.3; vLLM 0.28.0; 12 real 10.8k-token training turns, warm prefix, T=1.0/top_p .95/top_k 20)** — aggregate / per-stream gen tok/s at concurrency 8 and 16: bf16+LoRA (current) **180 / 22.6** and 325 / 20.3; merged bf16 ~185 / 23.4 (segfaulted after 34 s in CUDA-graph replay); merged bf16+MTP k=2 249 / 34.6 (crashed at 16 streams); merged online-FP8 326 / 40.7 and 557 / 34.8; **merged FP8+MTP k=2: 393 / 48.8 and 505 / 35.0** (acceptance length 2.35, per-position 0.77/0.58, 0 errors, sane 310-token turns with 38 % tool calls); ngram spec 154 / 20.7 (net loss); MTP k=3 crashes (device-side assert). Merge: 38 s on GPU, 400 modules, keeps the 15 `mtp.*` tensors.
- **Decision: serve evals as merged bf16 -> online FP8 + MTP k=2, tau2 `--max-concurrency 16`** (`remote_eval.sh` env `SERVE_MODE=merged QUANT=fp8 SPEC=mtp CONC=16`; defaults unchanged = old LoRA path at concurrency 8; also `THINKING=off`, `MAX_NUM_SEQS`, `CUDAGRAPH`). Expected: 97 tasks x 1 trial ≈ **1.2-1.5 h wall incl. setup (~$2)** instead of ~3.5 h; x 5 trials ≈ **4.5-5 h (~$7-8)** instead of ~15 h; ≈ 3-4x end-to-end (user-sim latency is the untouched remainder).
- **Quality caveats / gates before trusting a number from this config** (details in `research/inference_speed.md`): the bf16 merge keeps only ~80 % of adapter_v0's delta mass; online FP8 is not lossless; MTP preserves the sampling distribution but outputs still differ run to run. The 45.4 baseline was served by OpenRouter (precision uncontrolled), so comparisons are within noise only; always compare base vs student on the SAME serving config (one extra base run at ~$2) and record `eval_*/serving_config.json`. **Unresolved anomaly**: in the bench the current LoRA config and FP8-without-MTP returned 1200-token single-character degenerate reasoning on every request (thinking ON via the default template; the adapter was trained with thinking off), while merged bf16 / MTP configs and the live eval are sane — run the sample-dumping check (thinking on/off) and the 10-task dev set on the new config before the v1/final evals. Fallbacks: `SERVE_MODE=lora QUANT=fp8 SPEC=none CONC=16` (exact adapter, untested), then old path with CONC=16 (~1.8x aggregate, zero risk).
- **Follow-up bench (13:26–14:03 UTC, ~$0.8)**: LoRA path is sane (the earlier degenerate result was a one-off); **LoRA + MTP k=2 works**: 215 tok/s @8 (31/stream) and 323 @16 vs 143/239 without MTP; thinking-off turns are 2.4x shorter. **Eval config from now on: `SERVE_MODE=lora QUANT=none SPEC=mtp CONC=16`** (`launch_vast.sh OFFER remote_eval.sh 150 "-e ADAPTER=adapter_vX -e DATA_DIR=data_v1 -e SERVE_MODE=lora -e SPEC=mtp -e CONC=16 -e EVAL_SET=both -e TRIALS=1"`).
- **Lead's decision after review**: merged+FP8 (config E) would erase the adapter (delta ~0.3 % of weight magnitude vs FP8's ~6-12 % resolution; even the bf16 merge keeps only ~80 % of the delta mass), so evals stay on the unmerged LoRA path in bf16. Adopt concurrency 16 (1.8x aggregate measured) and test LoRA+MTP k=2 (supported in vLLM V1 since PR #21068) with output samples; the bench's degenerate-output result for the plain LoRA config is unexplained (the live eval shows sane ~435-token turns) and must be resolved before trusting any new config.

## Switchboard plan (2026-09-04 23:50 UTC, PLAN ONLY, nothing launched)
- User's idea: no-thinking phone-style agent on Qwen3.8-27B (NVFP4), synthetic traces from an explicit taxonomy of human phone-agent patterns, reference = Pipecat PhoneLLM Alpha 1. Researched with a 13-agent workflow (6 angles + 6 skeptics + synthesis, ~2.2M tokens); artifacts: `plans/switchboard_plan.html` (visual plan), `plans/switchboard_plan.json` (structured plan: 51 patterns in 12 groups with trigger/example/check, models, data stages S0-S4 with gates, training P1/P2/P3, serving, 6 evals, 12 risks, budget, 8-week timeline, 22 unverified items), `plans/switchboard_research_reports.json`.
- Key facts: PhoneLLM = bf16 full SFT of Nemotron 3 Nano 30B-A3B, thinking off, temp 0, English-only, proprietary data, PhoneBench 72.3 vs untuned Qwen3.8-27B 70.0 (Pipecat's own board); they trained bf16 then selective NVFP4 PTQ (-0.04 pts, FP8 KV -0.54). No official NVFP4 Qwen3.8-27B exists. Decision: P1 = bf16 LoRA r=128 all-linear -> merge -> selective NVFP4 PTQ (MLP W4A4, attn/GDN FP8, conv1d/lm_head/mtp bf16) with KL/eval/MTP-acceptance gates; P2 = QLoRA on frozen NVFP4; P3 = current bf16+LoRA+MTP. Teacher Qwen3.8-Max (thinking on, reasoning stripped), judge Sonnet 5 (+GPT-5.4 on eval), user-sim GLM-5.3-Flash + ASR noise. Lean budget ~$1.6k (GPU ~$200, API ~$1.35k), 8 weeks.
